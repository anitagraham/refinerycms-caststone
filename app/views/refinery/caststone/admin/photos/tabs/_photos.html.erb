<%
  content = content_tag(:div, class: 'wym_skin_refinery page_part', id:'page_photo_picker') do
    concat select :page, :photo_ids, Refinery::Caststone::Photo.all.map{ |p| [p.name, p.id, { 'data-url' => p.thumbnail({geometry: '250x'})} ] }, {},    {:multiple=>true, :class=>:multiselect, :id=>:photo_multiselect}
  end
%>
<%= content %>
